/** @type {import('tailwindcss').Config} */
export default {
  content: [
    './src/**/*.{html,svelte,js}', // Tambahkan path file yang akan menggunakan TailwindCSS
  ],
  theme: {
    extend: {},
  },
  plugins: [
    // Tambahkan plugin Tailwind lainnya di sini jika diperlukan
  ],
};
